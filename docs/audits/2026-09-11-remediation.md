# 博客整改记录

日期：2026-09-11。基线为 `13b613a32ca3c03abd30bacebd8df8769d05d50d`；基线审核与 Dario 官网/第三方主题对标保存在 [原始审核快照](2026-09-11-initial-audit.md)。

## 已落实的结构

保留 Astro 静态生成、Markdown、TypeScript、普通 CSS，保留 896px 容器及现有阅读设计。没有迁移框架或引入 CMS。

- 两个域名分别固定英文、中文；每页仅一个语言版本，切换链接指向同篇译文，不读取浏览器语言偏好。
- 构建入口依次生成 en/zh，再打包为英文根目录及内部 zh 子目录。元数据、RSS、sitemap、robots 与分享图均按域名生成。
- Markdown 翻译配对、日期与草稿规则统一校验，目录来自 Astro 实际渲染 headings。删除重复正文、正则目录解析、未使用 getter、旧语言同步脚本和失效组件名。
- 手机目录采用原生 dialog，修复 ESC、链接跳转、断点变化的焦点；灯箱有本地化可访问名称。
- 仅删除彩蛋真实照片，改为本地 2:3 中性 SVG，占位图沿用七次解锁、session 状态、叠放与 PhotoSwipe 灯箱。
- 头像使用响应式 AVIF/WebP/JPEG。字体来自本地 Fontsource，中文使用 unicode-range 分片，英文页不引用中文字体 CSS。普通阅读不需要加载完整彩蛋交互。
- 样式按目录、照片、加载、WIP 分开；移除全页加载遮罩，保留图片局部反馈。补充打印配色、窄屏溢出处理与 reduced-motion 分支。
- 发布必须使用同一 CI 验证产物；两语言健康标识一致，共享指纹资源跨版本保留，日常回退拒绝旧的单语言格式。

## 验证证据

| 检查 | 本地整改结果 |
|---|---|
| Astro + TypeScript | 44 文件，0 错误、0 警告、0 提示 |
| 行为测试 | 34 通过，136 断言 |
| 双语产物检查 | 8 通过，252 断言；包含实际产物主题脚本在 storage 抛错时的切换 |
| 构建 | 每语言 6 个 HTML 页面，另有 RSS/robots/sitemap |
| 依赖审计 | `bun audit --json` 返回 `{}`；frozen install 无锁文件变化 |
| 头像 AVIF | 320/640/960 宽版本分别 7,293 / 17,631 / 28,371 字节，替代初始 2,089,264 字节原图下载 |
| 首页彩蛋入口 | 3,412 字节；原入口 96,141 字节。完整依赖在解锁时导入，不是删去交互 |
| 字体预加载 | 每页从 14 项变为 0；字体仍由当前语言 CSS 按需请求 |
| Chrome 浏览器 | 英中首页、390/480px 无横向溢出、1600px 目录、ESC、目录标题焦点、双向断点焦点、主题、七次解锁、占位灯箱打开/关闭已检查 |
| Shell | HTTPS、bootstrap 安装/重跑健康、release、首次迁移文件与命令契约测试通过 |
| 真实 Nginx | 隔离 Linux 端口测试两域名、HTTP/www 跳转、共享资源、410、语言 404 和内部路径拒绝通过；完整 CI 再执行同一测试 |

语义产物检查直接解析静态 HTML，确认正文、元数据及原生语言链接无需 JavaScript 生成。storage 拒绝、图片错误重试、reduced-motion 状态使用针对性行为测试；没有将这些测试声称为完整的跨浏览器或真实触屏验证。未提供 Lighthouse/RUM 得分，没有对所有手势或操作系统打印驱动做穷尽验收。

## 部署与恢复

按用户最终要求通过已有 GitHub CI/CD 发布。服务器只通过腾讯云控制台进行一次必要配置准备；不手动 SSH 上传或切换网站版本。

当前生产部署用户无 sudo。一次性迁移桥接脚本按固定提交准备 root-owned 配置，并只临时允许三个无参操作：apply / restore / finish。GitHub 完成实际上传、激活、中文配置切换和双域名验证；初次失败恢复配置及旧布局，同时保留旧照片 410；成功撤销临时权限。流程、固定备份路径及恢复约束见 [HTTPS 运维说明](../../ops/HTTPS.md)。运行证据以 GitHub Actions 的具体提交及任务日志为准。

SSH 排查确认正确服务器地址是 `82.156.77.131`；原报错命令中的 `87.156.77.131` 不是本网站服务器。正确地址已完成连接，sshd 运行且 Fail2ban 当时没有封禁记录；没有为此放宽防火墙或修改 SSH 安全策略。

## 后续维护边界

站点不需要继续为减少少量重复代码引入框架。保持文章内容更新、补真实联系信息、定期依赖升级与真实用户性能观察即可。共享指纹资源暂不自动回收；保留五个内容版本不代表旧资源会同步删除。首次迁移之前的旧布局只保留作管理员灾难恢复，不作为日常回退目标。
