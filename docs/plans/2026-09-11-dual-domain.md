# 双域名博客整改实施计划

## Global Constraints

- Keep Astro static, Markdown, TypeScript, plain CSS, present layout width (896px container / about 848px text), desktop TOC breakpoint 1480px, homepage normal portrait, seven-activation Romantic Mode and PhotoSwipe interactions.
- siyuanxue.com always English; xuesiyuan.com always Chinese. www hosts redirect to their own apex. Retire xuesiyuan.com.cn and its www from website service and certificate renewal; do not alter unrelated DNS or domain registrations.
- One source; build selected locale only, then assemble English at dist root and Chinese at dist/zh. Public paths do not have locale prefixes. SITE_LOCALE accepts en or zh, defaults en locally, rejects invalid values. Build language mapping is authoritative, no localStorage/browser language override. Language links target counterpart article pathname without query/hash.
- Paired Markdown translations remain required, either draft excludes both. Incomplete publishable pairs, duplicate locale entries and mismatched dates must fail validation. Use actual Astro rendered headings, not independent regex parsing.
- Keep real homepage portrait, replace only secret /images/p-202.jpg with a neutral local 2:3 SVG placeholder and localized labels; remove old asset and references. Old URL must be denied with HTTP 410, including after rollback. Do not rewrite Git history.
- User explicitly requested execution including deployment. Latest user instruction: publish through existing GitHub CI/CD. Use Chrome Tencent OrcaTerm for the necessary one-time privileged configuration preparation; do not manually upload or switch production releases through SSH. Do not store authentication secrets in repo, reports or scripts. Verify actual server layout before mutation. No unrelated server changes.
- Main branch initially 13b613a32ca3c03abd30bacebd8df8769d05d50d. Original checkout contains an untracked audit report; preserve it. Work in /private/tmp/siyuanxue-dual-domain on codex/dual-domain-blog. Controller owns commits, shared task ledger, production and GitHub changes. Workers do not commit, push, deploy or spawn agents.
- Bun runtime /tmp/blog-audit-20260911/bun/package/bin/bun; Node /Users/milesxue/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node. Prefix PATH with their containing directories when executing scripts. Controller owns package installation and shared dependencies; coordinate dependency requests.

## Task 1: Frontend, content and locale build

Own src/, tests/ frontend tests, astro.config.mjs, package.json and tsconfig.json, plus build/verify scripts under scripts/. Do not modify ops/, .github/, documentation or .gitignore. Controller handles those. Read audit at /Users/milesxue/Documents/siyuanxue.com/docs/audits/2026-09-11-blog-audit.md for evidence.

Implement the approved frontend plan in these cohesive steps, using meaningful regression tests first where behavior changes:

1. Locale contract Locale='en'|'zh'; source Bi remains authoring convenience but every page receives only localized strings/content/headings. A shared locale helper resolves SITE_LOCALE and canonical origins. Remove client language storage/events and duplicated hidden DOM; language button is native cross-domain corresponding path anchor. Remove obsolete getter/pair fields and share pairing/schema logic. Keep essay/post route paths unchanged. Localized 404, wip noindex and excluded sitemap, WIP homepage items clearly marked unavailable. Article title unique h1, brand non-h1 on article pages.
2. render(entry).headings feeds TOC depth 2/3. Remove custom regex headings extraction and direct slugger dependency when unused. Native mobile dialog and desktop nav, focus management (open, close, ESC, navigation and breakpoint changes), hide TOC when no h2/h3. Shared list rendering is fine. Respect reduced motion, horizontal code/table overflow and print styles.
3. One build command independently builds en/zh in .build/<locale>, validates, assembles dist/ (English root) and dist/zh/ (Chinese). Both outputs retain their assets for local preview. Production package will contain a controller-generated release.json and matching __health per locale. Add locale-specific dev/preview commands, ensure each artifact uses correct site origin. Canonical, reciprocal en/zh-CN/x-default links, localized OG/Twitter metadata, BlogPosting metadata for articles, per-locale robots/sitemap/RSS. Add local default share PNGs in both languages (simple existing visual style, no remote image service), reference from metadata; controller can help asset generation.
4. Replace secret portrait with new neutral SVG 1200x1800 viewBox, update alt/dialog labels, remove old photo file; preserve all interactions and session-state semantics. Main portrait should be Astro responsive optimized AVIF/WebP/fallback at widths 320/640/960 and correct sizes. Thin initial controller; dynamically import PhotoSwipe/GSAP after actual unlock/open, handle duplicate clicks, load failure, and restored unlocked state. Pass only localized UI strings. Remove page-wide loading overlay, retain local image loaders. Split feature CSS out of base, rename BioExpand to Bio, update its imports.
5. Use locally installed @fontsource-variable/newsreader and @fontsource-variable/noto-serif-sc, locale build-specific CSS entry, Unicode-range segmented CJK, no indiscriminate font preload, retain current serif fallbacks. Avoid adding a separate Python font build pipeline. Fix all TS errors including PhotoSwipe callback this context, unsupported option declaration, animation discriminants, Bun test types. Add @astrojs/check and @types/bun checks. Upgrade Astro within supported 7.x to patched >=7.2.8, sharp >=0.35.4 and fix available transitive advisories; controller installs requested dependencies after package.json changes. Preserve plain CSS and existing visual design.
6. Write behavioral tests for locale/content validation and real Markdown headings edge cases (fenced code, h1/h2 repeated names, underscore, entities, setext). Update output tests for both artifacts and actual semantic HTML/links, not implementation string snapshots. Keep existing meaningful Romantic tests. Provide check, test, build, test:output scripts usable by a shared CI verification workflow; ops tests remain controller-owned. Do not claim runtime UI testing without performing it; controller handles browser acceptance.

Report changed files, commands/results, any concerns and needed controller integration in the designated task report. Do not expose unrelated source or secrets.

## Task 2: Operations and CI

Controller owns ops/, .github/, .gitignore and docs. Extend release validation to both locale roots and a versioned release manifest, same __health SHA. Shared append-only hashed assets seeded from current and retained releases; no auto GC in first version. Use existing atomic current/previous and five successful releases; daily rollback only accepts bilingual format after cutover. Keep archive traversal/checksum checks.

Nginx English root remains current, Chinese root current/zh, no /zh public access on English domain. Shared assets served by both, HTML revalidation. Old secret photo exact URL returns 410 on active hosts. HTTP/www canonicalize within corresponding language, preserve request URI and ACME exceptions. Unknown/retired hosts never expose website. Remove retired domain from active templates/whitelists/tests/docs; mark historical docs obsolete. Fix bootstrap re-run health comparison and config sync. Add or update real shell behavior tests and Nginx validation in Linux.

CI and deployment use same validation workflow and exact tested artifact. Both domains verified after activate and rollback, complete SHA comparisons. Add explicit check mode for pre-migration readiness; no accidental early main auto deployment.

## Task 3: Integration, review and production

Read actual Nginx and filesystem through SSH first (prior current path lookup failed), identify existing website and certificates, take scoped backups. First remove photo and deny its URL, establish recovery baseline. Pause overlapping automatic deploy, prepare the root-owned fixed-SHA migration bridge through Chrome, and use GitHub CI/CD to validate, upload, atomically switch the release and apply the tested Nginx configuration. Verify both domains, www, HTTP, TLS, SEO, image removal, retired domain rejection and health SHA. Restore config and release together on migration failure. Stop legacy .cn Certbot renewal only after its Nginx references are removed; preserve unrelated sites/services/DNS. Prefer reusing present deployment mechanism if compatible, migrate carefully if repo deployment has never been installed.

Use browser to verify desktop/mobile, language links, no-JS content, theme with unavailable storage, modal focus, Romantic unlock/lightbox/load error, reduced motion and resource requests. Review full diff, run complete appropriate checks, preserve test evidence. Integrate source coherently, update audit and docs with actual results and report limitations honestly.
