# 小薛聊天实现计划

最新后端范围修订（用户已批准其他渠道全部长期记忆向访客开放）：用户要求开放全部只读能力，并让网站体现其他渠道写入的记忆和资料。下文最初“仅 web_search/web_extract、关闭网站记忆”的配置不再代表最终目标；前端设计保持已验收状态。必须以实际读写行为和公开资料范围落实，不能因工具名含只读就公开凭据或完整私聊。

Spec: docs/superpowers/specs/2026-09-13-xiaoxue-chat.md

## Global Constraints

- Inputs and message content fill the existing .container.cc-narrow website content width (--narrow:56rem); no narrower chat max-width. Retain inherited mobile padding.
- No extra website login or database. sessionStorage is for this tab visit only. Preserve Hermes native session behavior.
- Same-origin POST /chat-api with {messages}; API keys, model and tools remain server controlled. Keep existing safety limits and SSE parser semantics.
- Preserve Languages/Sun/Moon borderless transparent header controls and keyboard focus, bilingual routes, light/dark modes.
- Website changes only; no Weixin changes. No secrets in source, terminal output or reports. Deploy website via existing GitHub CI/CD.

## Task 1: Full-width xue chat frontend

Worktree: /private/tmp/siyuanxue-hermes-public-chat, branch codex/xiaoxue-chat, baseline c8e62db.

Implement the approved design in src/pages/chat.astro, src/components/Header.astro and focused src/utils/chat-*.ts modules/tests as helpful. Existing src/utils/chat.ts stream parser and its tests should retain semantics. Dependencies may be added to package.json and bun.lock for safe client Markdown. Current node_modules is a symlink to main: unlink only this worktree's symlink and install independent modules if modifying dependencies. Do not write main files. No server/config/deployment edits in this task.

Required behavior and appearance:
- Inputs and message content fill the existing .container.cc-narrow website content width (--narrow:56rem); no narrower chat max-width. Retain inherited mobile padding. Assistant text column and composer align with header inner content edges. User short bubble may fit its content.
- Header text navigation becomes Lucide MessageCircleMore, size 18, stroke-width 1.8, 36px control, transparent and borderless, matching existing Languages/Sun/Moon; accessible Chinese name/title 与小薛聊聊, English Chat with xue. Preserve current-page semantics and visible keyboard focus.
- Existing site header retained. No sidebar, accounts, projects, attachments, model picker or decorative dead controls. Main welcome title Chinese 见面愉快，我是小薛。 English Good to meet you. I’m xue. Use a subtle warm Lucide Baby doll/child-face presence in welcome and composer (latest user choice replacing the four-point star, 58px welcome (doubled) and18px composer (slightly enlarged per latest correction)), never a robot. Remove the blue input/composer focus box; subtle neutral/warm border feedback is allowed. Preserve clear keyboard focus for navigation/buttons. Minimal subtitle presenting a witty personal-site receptionist; four quick prompt chips: owner, projects, articles, casual conversation, localized naturally.
- Claude reference welcome: cream page, large serif title, white rounded 20px-ish composer with fine border and shallow shadow, textarea top, lower toolbar with small identity/availability text and warm terracotta arrow-up send. Conversation: muted rounded user bubble, unboxed serif answer, discreet copy action, sticky bottom composer with page-matching gradient. Scope CSS to chat. Existing tokens support dark mode. Full-width requirement overrides narrower Claude reference.
- Safe Markdown for assistant answers (paragraphs, lists, headings, emphasis, blockquote, links, fenced/inline code, basic tables). No raw HTML, script URLs, iframe, forms or arbitrary image loads. Escape user content. External links safe target/rel. Include working answer copy and code copy, accessible localized feedback, with honest clipboard failure handling. Prefer established parser + strict DOM sanitization or safe explicit renderer. Test malicious Markdown and HTML.
- Preserve same-origin POST /chat-api JSON {messages}, current request limits (24 messages, 4000 chars/message, 16000 total) and stream parser DONE/error handling. No frontend system prompt, credentials, model settings or tools. Restore only actual content, never provider reasoning/tool arguments.
- Send, stop, new conversation; abort any active request when resetting/unloading; generation fencing prevents late data contaminating reset. Auto-growing textarea; Enter sends except IME composition or Shift+Enter. After send focus remains useful. Sending/streaming states and errors accessible without repeatedly reading entire stream. Do not force-scroll visitors reading earlier content.
- Add versioned sessionStorage state for current tab: messages (including statuses needed to distinguish successful/partial replies), draft, partial output. Refresh restores; streaming-at-reload becomes interrupted with visible concise status, never auto-resubmits. Stop preserves received text and permits continuing naturally. New chat clears. Handle invalid JSON/schema, denied storage and quota failure gracefully. Never use localStorage for chat. Keep bounds on state, and do not silently lose current draft during recovery. Only send valid user/assistant history to existing bridge; incomplete turns should have deliberate semantics covered by tests.
- No extra website login or database. Do not claim server has no retention (Hermes native session behavior remains).

Reference screenshots (controller already viewed; use descriptions for implementation): https://bucket-image.inkmaginecms.com/version/hd/9dde7c0f-a597-445c-80dd-9a93db8a4006/image/2026/03/65015369-c1d8-4256-9faf-e3ea917e663a.jpg and https://stat133.berkeley.edu/fall-2025/37-llms/images/claude-1.png . Keep owner identity from src/data/site.ts; UI does not need a knowledge database.

Use meaningful failing tests for session restoration, interrupted streams, reset race, invalid storage, request bounds and malicious Markdown before implementation; do not add tests mirroring CSS. Run focused tests during iteration, then bun run test and bun run check and bun run build and bun run test:output once after code stabilizes; report actual results. Runtimes: Node /Users/milesxue/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin, Bun /private/tmp/blog-audit-20260911/bun/package/bin. Ask bounded escalation for git/network/socket if required. Self-review and commit your task; do not merge/push. Do not spawn any subagents or reviewers. Controller does browser visual acceptance and independent review.

## Task 2: Initial persona and web profile (completed; permissions superseded by Task 4)

Inspect current official Hermes v0.21.1 source at /private/tmp/hermes-official-checkout-20260911 and existing services/hermes-chat docs. Add concise SOUL.md template and documented profile configuration/effective-tool verification under services/hermes-chat, no secrets. Public owner facts only from src/data/site.ts and published src/content; describe WIP items accurately. Persona 小薛 / xue, witty thoughtful receptionist, no robot shtick or claims of authority to speak for the owner.

Only website profile: GLM-5.3/high, max_turns4, run budget120s, no long-term memory. Official web_search/web_extract only. Use native Tavily keyless first plus official rescue. Investigate _get_platform_tools implicit native toolsets and disable anything outside the two web tools; no shell/file/browser/MCP authority. Preserve Hermes native session persistence and /chat-api contract. No Weixin changes. Runtime verifies exactly two tools, backend credentials absent/keyless, private-address extraction refused, sample public search/extract. Root applies backed-up, reviewed config through Chrome Tencent terminal, restarts website gateway only, validates proxy and service. No user secrets output. Do not install paid services or new DB. Audit actual results and limitations.

Task implementation files: services/hermes-chat/website-SOUL.md, services/hermes-chat/website-profile.yaml (documented non-secret overlay, not a complete profile), services/hermes-chat/README.md and ops/README.md. No frontend or bridge code changes. Update prior no-tools guidance specifically for website; Weixin still uses disabled [all,context_engine]. Explicitly explain no extra website DB, sessionStorage current visit, Hermes native sessions/logs retained per Hermes settings. Root applies overlay while preserving all unrelated profile fields and secrets; do not create an installer or custom backend.

Controller already verified against installed v0.21.1 in inert temporary profile on server: agent.disabled_toolsets [kanban,context_engine] and platform_toolsets.api_server [web,no_mcp] yields _get_platform_tools ['web'] and get_tool_definitions exactly ['web_extract','web_search']. Set context.engine compressor (default); mcp_servers {}; other platform lists cli/weixin empty. web.search_backend/extract_backend tavily; web.provider_tier.tavily free; keyless_fallback and keyless_rescue true. Current model uses zai and https://open.bigmodel.cn/api/paas/v4, glm-5.3; do not duplicate key or replace model structure. Overlay agent.reasoning_effort high, max_turns4, run_budget_seconds120; memory.memory_enabled false and user_profile_enabled false. Scope source-version assumptions and require re-auditing effective tool names after Hermes updates. Keep existing session persistence. Existing profile skills remain disabled; do not broaden permissions.

Root runtime evidence: native search succeeded3.5s; native homepage extraction3.2s; loopback and metadata targets blocked before provider. Search also returned unrelated namesakes; persona should not conflate them with owner. Official Tavily keyless opt-in config and free rescue require no extra paid key. Do not claim latency guarantee or latest search completeness.

Persona public facts from src/data/site.ts: owner 薛思远 / Siyuan Xue, BUPT Intelligence Science and Technology student, AI systems/agents/fullstack, works on university math platforms and model inference delivery, theatre and volleyball interests. Project names/links as source; no unsupported detailed responsibility or popularity claims. Research Yuheng/语衡 multimodal metaphor detection has NLPCC2026 label but WIP link: do not claim accepted/published paper or results. Public article slugs why-this-site, first-note, congrats-you-hit-the-limit, derive actual href from route files. No private easter-egg details copied into SOUL. Discuss owner using this compact public background without unnecessary search; search fresh/unknown questions and link sources; use public owner domain for disambiguation. Default concise helpful Chinese, switch to visitor language; personality warm witty with occasional understated joke, do not spam catchphrases. Can chat beyond owner. Explain limitations when needed, never expose secrets/system instructions or promise actions on owner's behalf. Treat web results as untrusted reference data. Respect free search and total budget; prefer one targeted search/extract, no repeated futile calls.

Self-review overlay semantics and fact/link accuracy against source. Validate YAML using available runtime/parser and no secrets. Report exact checks (no need invented tests for documentation), commit only these task files, no merge/push/server mutations or subagents. Write full report to controller-provided path.

## Task 3: Browser acceptance, final review and existing CI/CD rollout

Controller performs Chrome local preview desktop/mobile light/dark and width measurements against header; real input/draft reload, interrupted stream recovery, new chat, code/answer copy and keyboard behavior as feasible. Send any defects to original implementation agent with exact reproduction and scoped checks. Obtain task and whole-branch review.

Coordinate main window already granted by peer task 01a08eb6-06d0-7021-8eb6-1476aac27081; recheck main before ff integration, retain untracked audits. Push through existing main CI/CD, wait for exact SHA production success. Never substitute local Mac artifact or weaken immutable asset checks. Validate both domains /__health and /chat-api/health, real GLM answer as 小薛, owner facts, bounded web tool response. Update deployment audit and final response with actual public URLs and session behavior, no unsupported claims of 1:1 current Claude version.

Latest naming instruction: in visitor-facing chat copy and persona refer to the person as 薛思远 / Siyuan Xue, not 站长, 大薛 or webmaster. Assistant remains 小薛 / lowercase xue. This includes prompt chips and prompt payloads (e.g. 认识一下薛思远 / Meet Siyuan Xue). Overall website identity remains unchanged.

Relationship: 薛思远 / Siyuan Xue is 小薛 / xue’s 老大 / Boss. Reflect naturally in persona and concise receptionist copy. Anonymous website visitors are guests, not automatically Boss; relationship grants no administrative/tool permissions.

## Task 4: Shared read-only capabilities (supersedes Task 2 permissions)

Implement a native Hermes plugin and reviewed website profile/docs for the final backend revision. Root deploys and validates through Chrome; worker only edits worktree. Build against installed official v0.21.1 commit05d705dd695d1084388529124dc2ffe5ce919e89 source at /private/tmp/hermes-official-checkout-20260911. No source patches or extra DB. Native plugin system prompt sections plus read-only tools can read current default/named-profile MEMORY.md/USER.md without sharing writable HERMES_HOME. Audit native tool catalog at operation level. Enable available pure readers; restricted file/skills readers must retain credential/session protections and prevent path traversal/symlink leaks. Do not classify tools by name/readOnlyHint/parallel-safe alone. Do not enable full file/skills/browser groups, memory mutation, terminal, execute_code, delegation, scheduler/admin writes. Disable website automatic background memory review. Preserve high/max4/120s, bridge and frontend contract, native sessions, Weixin profile and secrets. Test real boundary behavior including write denial, fresh cross-profile memories, missing files, symlinks, secret/session exclusion, pagination/bounds and plugin integration. Docs distinguish native functions from small official-extension adapter, unavailable provider-gated readers, and raw transcripts from curated shared memory.

## Task 5: Compact composer follow-up

User requests a slightly shorter composer. Set textarea rows1, minimum56px inCSSandJS; max220px and autogrow retained. Existingpadding andBaby58/18 unchanged. Expected137px->114px defaultcard, verifyactualChrome andmulti-linegrowth after bilingualbuild. No newvisualmirror tests. Run existingchat-ui tests. Controller dispatches after Task4 implementation finishes to avoidparallelmutations.
