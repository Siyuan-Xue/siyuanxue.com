# Public Hermes Chat Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development; execution and deployment are already approved. No further user approval is needed for the specified implementation or publication.

**Goal:** Publish chat-only Hermes access on both personal websites and configure the official multi-user Weixin channel.

**Architecture:** Static Astro UI calls a constrained same-origin Node bridge, which calls the official Hermes API with a secret stored only server-side. Two new Hermes profiles keep website/Weixin state apart from personal Hermes; public tools and memory are disabled.

**Tech Stack:** Existing Astro/Bun/TypeScript, Node standard library HTTP/fetch, Nginx, systemd, installed Hermes v0.21.1.

**Spec:** docs/superpowers/specs/2026-09-11-public-hermes-chat.md

## Global Constraints

- Preserve GLM-5.3 high, streaming, private default profile, existing bilingual releases, HTTPS behavior and `/images/p-202.jpg` denial.
- No new npm packages; no secrets in repository, front-end, test output or reports.
- Existing base commit b2b4519; isolated branch codex/hermes-public-chat.
- Node: /Users/milesxue/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node; Bun: /private/tmp/blog-audit-20260911/bun/package/bin/bun. Existing node_modules is linked from main workspace.

### Task 1: Website chat, constrained bridge and deployable operations

**Files:** Create services/hermes-chat/server.mjs, services/hermes-chat/README.md, src/pages/chat.astro, src/utils/chat.ts, tests/hermes-chat.test.ts and any narrowly scoped frontend behavior test; modify src/components/Header.astro and locale copy/style as needed; create ops/install-hermes-chat.sh, ops/hermes-chat.service, ops/nginx/hermes-chat.conf; modify both domain templates to include the chat routes safely; document exact installation in ops/README.md. Do not change unrelated audit files or current release helper semantics.

**Interfaces:** UI POST /chat-api with only `{messages:[{role,content}]}`, consumes standard OpenAI SSE `data:` chunks with choices[0].delta.content and [DONE]. Bridge reads HERMES_API_KEY, HERMES_API_URL default http://127.0.0.1:8642/v1/chat/completions, CHAT_HOST=127.0.0.1, CHAT_PORT=8643, ALLOWED_ORIGINS=https://siyuanxue.com,https://xuesiyuan.com. Public status route `/chat-api/health` contains no sensitive information. All payload and limit constraints are in the spec, verbatim.

- [ ] Write and run failing behavioral tests for rejecting system/tool roles, forged overrides, oversized/invalid sequences, cross-origin calls, concurrency and rate limits, streamed upstream output, timeout/disconnect cleanup, and no error/credential leakage. Use a controlled real HTTP upstream in tests, no paid model calls. This is new behavior; initial missing-module red is acceptable evidence followed by assertion reds as necessary.
- [ ] Implement bridge with explicit start/export boundary that tests can launch on an ephemeral loopback port. Use bounded admission state and cleanup; no shared public session ids or upstream header passthrough.
- [ ] Implement accessible bilingual UI using the existing Base layout and styles; prevent model HTML execution, handle partial SSE frames, stop/new/error/empty states, IME and mobile. Add chat entry to both locale headers.
- [ ] Supply idempotent install helper/service and nginx route include. Installer must preserve all unrelated server config, validate exact input paths and nginx config, and fail closed if secret is missing. No secrets need be generated by this agent; controller supplies the profile key on server. `systemd` bridge service uses env file `/etc/hermes-chat.env` mode 600. Nginx route block lives in `/etc/nginx/snippets/hermes-chat.conf` included by both HTTPS apex servers and the existing public-IP server if desired. Keep original default-deny hosts.
- [ ] Run focused tests, `bun run check`, full frontend tests, both locale builds/output checks, relevant ops tests. Capture existing baseline failures separately. Commit only task changes, self-review, write report. No push, publication or server operation by this worker.

### Task 2: Remote official profiles, Weixin QR and public deployment

**Files:** Server-only Hermes profiles/.env/config.yaml, installed bridge/unit/nginx snippet, standard release archive; tracked spec/plan completion documentation.

**Interfaces:** Task 1 exact install helper and bridge env contract, Hermes official gateway/API server, Tencent Chrome terminal. Controller owns every remote action and credential.

- [ ] Configure website-chat and wechat-public from fresh no-skills profiles, copy only existing GLM credential server-side, set toolsets empty, memory flags false, cap iterations/time and persist owner-only Weixin admin command gate.
- [ ] Use official Weixin setup QR, ask owner to scan and confirm, record only account/user identifiers; configure open DMs as requested. Do not describe the login QR as a reusable invitation QR.
- [ ] Start official profile services and verify actual API tool exposure, model response and Weixin status.
- [ ] Review Task 1, package its exact tested commit and backend artifacts, transfer using Chrome terminal/file manager, back up nginx config, install bridge, run nginx -t, activate website release with existing helper, and verify HTTPS chat plus all prior health/locale/denial checks.
- [ ] Verify invalid/over-limit calls and public cross-user isolation without exposing raw key/session data; report actual completion and any phone-side sharing step still needed.
