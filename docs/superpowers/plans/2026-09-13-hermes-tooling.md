# Hermes tools, common identity, and Weixin permissions

User-approved specification: xue is a channel-independent assistant for Siyuan Xue (Boss). All existing chat surfaces use the same identity and curated knowledge about Boss. The bound owner's Weixin DM has full tools; future other Weixin users and public website visitors are read-only. Consult official Hermes docs, enable as many genuinely usable tools as possible, especially web search, validate current date/time, and inspect the existing Weixin history for problems.

Baseline: 610803057a7fd5b5d0f7d81f4bec9e590eca831c. Worktree /private/tmp/siyuanxue-hermes-tooling. Installed Hermes v0.21.1 source 05d705dd695d1084388529124dc2ffe5ce919e89, mirrored at /private/tmp/hermes-official-checkout-20260911. No upstream source edits. Existing website-readonly plugin exposes seven safe readers and disables automatic tool deferral. Existing Weixin profile has no tools and memory disabled, causing plain-text fake TOOL/MEMORY markers and incorrect date claims; database contains nineteen messages and zero tool calls. System Asia/Shanghai with NTP synchronized.

Global constraints: never print credentials, publish raw Weixin conversations, or give unverified guests write/terminal access. No frontend/layout changes or unrelated repository files. Keep GLM-5.3/high. Preserve history and backups. Existing website gateway remains independently isolated. Use official configuration, profile routing, and plugin APIs. User already authorized server tool setup and owner-only full privileges; do not ask redundant approvals. No new paid accounts or subscription purchases.

## Task 1: Read-only clock, common identity, and public-profile verification

Implement only services/hermes-chat and tests/test_hermes_readonly.py, plus a focused operations document if needed. Do not touch frontend, main branch, existing audit files, live server, or deploy/push. Commit branch after tests; no subagents.

1. Extend website-readonly with one argument-free current_datetime tool. Return fresh ISO local time, timezone, UTC time, epoch seconds and weekday from the actual clock each call. Use official hermes_time.now in register(), injected into Readers with a default callable for isolated stdlib tests. No subprocess, shell, network, files, or cached timestamp. Reject extra arguments using the existing strict schema validation. Only aware datetime values; errors must not fabricate a result. Add the tool to manifest and bump version.
2. Permit registration ONLY in profiles/website-chat and profiles/wechat-public. These are the dedicated public read-only profiles; do not permit default or any owner profile. Existing tool veto must still deny every writer, terminal, native memory mutations, raw session_search, and discovery bridges. Exactly eight final model-visible tools: current_datetime, web_search, web_extract, shared_memory_list, shared_memory_read, knowledge_list, knowledge_read, knowledge_search. Preserve all filesystem/redaction boundaries.
3. Replace channel-bound website-SOUL.md with canonical assistant-SOUL.md, using git rename as appropriate and updating current operational references. Universal Chinese-first identity (follow interlocutor language): 小薛 / exact lowercase xue; owner 薛思远 / Siyuan Xue, Boss/老大. Not inherently a website receptionist, not owner himself, never assume every guest is Boss. Stable personal facts should come from curated memory rather than embedding a long static biography in SOUL. Explain that available tools/verified runtime identity govern permissions; never simulate TOOL/Bash/MEMORY markers as execution; claim success only after real tool result. For current time use current_datetime or actual terminal date, never old messages; for weather/current facts use web tools with source/date and report failure honestly. Use native memory tool only when actually available, do not falsely claim persistence. No web-only tool prohibitions in this universal identity, because owner's profile legitimately has full tools. No secrets or transcript quotations in SOUL.
4. Add timezone: Asia/Shanghai to website overlay. Add a matching wechat-public-profile.yaml overlay: same read-only plugin/memory-off/background-review-off/direct schemas, weixin [web, website_readonly, no_mcp], api_server and cli empty; no credentials, identity IDs, platform login/transport changes. Same four turns /120s budget as website.
5. Update verify-readonly.py to audit final schemas (raw separate), validate timezone, and support website/Weixin public fixtures with the appropriate profile name and platform. Keep missing-plugin result exactly two web tools. Preserve actual-native auto-deferral regression, source memory freshness, secret redaction, write veto. Add clock-native comparison and expected eight names. Existing default invocation compatibility should remain.
6. Tests: first reproduce missing clock; then verify advancing date across midnight with injected aware clock, fresh real clock bounds, timezone/UTC/epoch consistency, extra-argument rejection, and writer denial. Verify public-profile identity scoping where feasible using a fake official context; full installed integration is controller-owned. Run stdlib boundary suite and py_compile/diff checks. No full frontend builds needed. Update READONLY.md accurately for universal identity, clock, and two public profiles; avoid unsupported claims of universal third-party provider support.

Report file includes changed files, test commands/results, commits, concerns. Final message only status, commits, short tests summary, concerns.

## Task 2: Server configuration and real integration (controller)

Back up config, SOUL and state through safe copies/SQLite backup. Inspect bound account owner ID without exposing token; prove match to sender and configured admin. Use official default-profile multiplex gateway with allowlist only wechat-public and xue-owner, explicit owner chat route to xue-owner and catch-all Weixin route to wechat-public; default Weixin tools empty to fail closed. Leave website-chat excluded and separate. Transport credentials stay in wechat-public; owner gets only existing model credentials and strict own-user authorization. xue-owner enables official broad core toolsets, memory and recall, terminal/file/code/skills/browser/planning/delegation/cron where genuinely available. Install supported browser dependencies and test. Set free Tavily search/extract with native rescue, tune bounded timeouts. Shared canonical SOUL symlink for all existing profiles; curated Boss facts in owner's USER.md, incorporating only stable directly supplied preferences, not fabricated dates or fake memory markers. Preserve prior nineteen messages for owner recall. Stage and verify schemas/routes/memory isolation before switching gateways. Rollback on failed startup/acceptance.

## Task 3: Acceptance and integration (controller plus review)

Review Task1 diff, run actual native fixture/installed audits, then broad final review. Actual model tests must prove real structured tool calls for date/time and search (not plain text), compare date/time against NTP-synchronized host, inspect search results/sources, and verify owner memory write/read and prior-history recall without exposing private transcripts to guests. Test owner-vs-new-user profile resolution and guest write veto using native routing/dispatch. Check website endpoint and Weixin gateway healthy. Coordinate any main integration with concurrent project task; no unnecessary frontend rebuilds for server-only operation. Report configured/working tools versus tools needing extra credentials and residual platform limitations.
